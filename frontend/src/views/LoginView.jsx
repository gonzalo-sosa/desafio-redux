import AuthView from './AuthView';
import LoginForm from '@/components/login-form';

const LoginView = () => {
  return (
    <AuthView>
      <LoginForm />
    </AuthView>
  );
};

export default LoginView;
